# Local Development Environment

## Default Workflow
- docker compose for local dependencies
- `make dev` as the primary entry point
- Nx used to run API and workers locally

## Kubernetes Locally
- Local Kubernetes (kind or k3d) is optional
- Introduced as a later development stage
